export interface Game {
    id?: number;
    title?: string;
    description?: string;
    image?: string;
    create_at?: Date 
}

//la ',' se usa para los objetos y el ';' se una para las definiciones de las propiedades de las interfaces 